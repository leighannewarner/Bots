# betty_startup.py
# These startup files ensure that python knows
# where the root of the module starts so we can
# actually import our files
# The act of importing a script also runs it

from betty import betty_client